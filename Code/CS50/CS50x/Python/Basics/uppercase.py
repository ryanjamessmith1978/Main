from cs50 import *

before = input("Before: ")
after = before.upper()
print("After:", after) ## DON"T need '+' here because it's already a string
print(f"After: {after}")

#for c in before:
#    print(c.upper(), end="")
# print()
