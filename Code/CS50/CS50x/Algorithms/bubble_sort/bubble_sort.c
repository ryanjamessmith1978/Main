#include <cs50.h>
#include <stdio.h>
#include <string.h>

// Bubble Sort - O(n^2)

/*
Repeat n-1 times
    For i from 0 to n-2
        If num[i] and num[i+1] are out of order // if none are out of order, best case of O(n)
            swap num[i] and num[i+1]
*/

// Best Case - O(n) with caveat (with if statement)

int main(void)
{

}