#include <cs50.h>
#include <stdio.h>
#include <string.h>

// Merge Soft - O(n log n)
// Height of Shelves represents log(n)
// Width of one single shelf is (n)

int main(void)
{
    /*
    If only one number
        Quit
    Else
        Soft left half of numbers
        Soft right half of numbers
        Merge softed halves (compare the begining of each list, add the lower number into new array)
        2457 0136
        01234567
    */
}