#include <cs50.h>
#include <stdio.h>
#include <string.h>

/** Bubble Sort **

Repeat n-1 times
    For i from 0 to n-2
        If num[i] and num[i+1] are out of order
            swap num[i] and num[i+1]
*/

// O(n^2) runtime for worst case scenario
// O(n) runtime for best case scenario (with caveat, if condition (no swaps, quit))
// Not much memory used

int main(void)
{

}