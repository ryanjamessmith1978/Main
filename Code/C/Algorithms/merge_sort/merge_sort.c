#include <cs50.h>
#include <stdio.h>
#include <string.h>

// Merge Soft - O(n log n)
// CS50 Shelve example - uses more memory but less runtime
// Height of Shelves represents log(n)
// Width of one single shelf is (n)
int main(void)
{
    /*
    If only one number
        Quit
    Else
        Soft left half of numbers
        Soft right half of numbers
        Merge softed halves (compare the begining of each list, add the lower number into new array)
        2457 0136
        01234567
    */
}

//** Big 'O' ALGORITHM EFFECIENCY **
//
// O(n^2) - bubble sort && selection sort
// O(n log n) - merge sort
// O(n) - linear search
// O(log n) - binary search
// O(1) - go to answer in one single instruction
//